#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    auto y = 356; 

    cout << "value : " << y << " of type " << typeid(y).name() << endl;
    return 0;
}